<Window x:Class="TrackStop.ViewModels.EditAlbumWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Редактирование альбома" Height="700" Width="800"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="✏️ Редактирование альбома" 
                   FontSize="24" FontWeight="Bold" 
                   Foreground="#FFDA0000" 
                   Margin="0,0,0,20"/>

        <!-- Информация об альбоме -->
        <Border Grid.Row="1" 
                Background="#F5F5F5" 
                CornerRadius="10"
                Padding="20"
                Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Border Width="120" Height="120"
                        CornerRadius="5"
                        BorderBrush="#CCCCCC" BorderThickness="1"
                        Margin="0,0,20,0">
                    <Image x:Name="AlbumCoverImage" 
                           Stretch="UniformToFill"/>
                </Border>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBox x:Name="AlbumTitleTextBox" 
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             FontSize="16" FontWeight="Bold"
                             Margin="0,0,0,10"/>

                    <StackPanel Orientation="Horizontal">
                        <Button Content="🖼️ Сменить обложку" 
                                Click="ChangeCoverButton_Click"
                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                Margin="0,0,10,0"/>
                        <TextBlock x:Name="CoverPathText" 
                                   Text="" 
                                   Foreground="Gray"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="2" 
                            VerticalAlignment="Center"
                            Margin="20,0,0,0">
                    <Button Content="💾 Сохранить" 
                            Click="SaveAlbumButton_Click"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="#FFDA0000" Foreground="White"
                            Margin="0,0,0,10"/>
                    <Button Content="🗑️ Удалить альбом" 
                            Click="DeleteAlbumButton_Click"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            Foreground="Red"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Управление песнями -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                <TextBlock Text="Песни в альбоме" 
                           FontSize="18" FontWeight="Bold"/>
                <Button Content="➕ Добавить песню" 
                        Click="AddSongButton_Click"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Margin="20,0,0,0"/>
            </StackPanel>

            <ListView Grid.Row="1" x:Name="SongsListView">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Название" Width="200">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"
                                             BorderThickness="0"
                                             Background="Transparent"
                                             FontSize="14"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Исполнитель" Width="150" DisplayMemberBinding="{Binding Artist}"/>
                        <GridViewColumn Header="Длительность" Width="80" DisplayMemberBinding="{Binding Duration}"/>
                        <GridViewColumn Header="Действия" Width="150">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="✏️" 
                                                Tag="{Binding Id}"
                                                Click="EditSongButton_Click"
                                                Style="{StaticResource MaterialDesignIconButton}"
                                                Margin="0,0,5,0"/>
                                        <Button Content="🗑️" 
                                                Tag="{Binding Id}"
                                                Click="DeleteSongButton_Click"
                                                Style="{StaticResource MaterialDesignIconButton}"
                                                Foreground="Red"/>
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="✅ Готово" 
                    Click="DoneButton_Click"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Background="#FFDA0000" Foreground="White"
                    Margin="0,0,10,0"
                    Width="100"/>
            <Button Content="❌ Отмена" 
                    Click="CancelButton_Click"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Width="100"/>
        </StackPanel>
    </Grid>
</Window>