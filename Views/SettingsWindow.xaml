<Window x:Class="TrackStop.ViewModels.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Настройки" Height="400" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="⚙️ Настройки" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
            <TextBlock Text="Настройте приложение под себя" FontSize="14" Foreground="Gray"/>
        </StackPanel>

        <!-- Контент настроек -->
        <StackPanel Grid.Row="1">
            <!-- Внешний вид -->
            <materialDesign:Card Margin="0,0,0,15" Padding="15">
                <StackPanel>
                    <TextBlock Text="🎨 Внешний вид" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

                    <!-- Выбор темы -->
                    <StackPanel Margin="0,0,0,10">
                        <TextBlock Text="Тема" FontSize="16" Margin="0,0,0,5"/>
                        <StackPanel Orientation="Horizontal">
                            <RadioButton x:Name="LightThemeRadio" 
                                         Content="Светлая" 
                                         GroupName="Theme"
                                         Margin="0,0,15,0"
                                         Checked="LightThemeRadio_Checked"/>
                            <RadioButton x:Name="DarkThemeRadio" 
                                         Content="Темная" 
                                         GroupName="Theme"
                                         Checked="DarkThemeRadio_Checked"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Цвет акцента -->
                    <StackPanel>
                        <TextBlock Text="Акцентный цвет" FontSize="16" Margin="0,0,0,5"/>
                        <ComboBox x:Name="AccentColorComboBox" 
                                  Width="200"
                                  SelectionChanged="AccentColorComboBox_SelectionChanged">
                            <ComboBoxItem Content="Красный" Tag="#FFAB0000" IsSelected="True"/>
                            <ComboBoxItem Content="Синий" Tag="#2196F3"/>
                            <ComboBoxItem Content="Зеленый" Tag="#4CAF50"/>
                            <ComboBoxItem Content="Фиолетовый" Tag="#9C27B0"/>
                            <ComboBoxItem Content="Оранжевый" Tag="#FF9800"/>
                        </ComboBox>
                    </StackPanel>
                </StackPanel>
            </materialDesign:Card>

            <!-- Профиль пользователя -->
            <materialDesign:Card Padding="15">
                <StackPanel>
                    <TextBlock Text="👤 Профиль" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Текущий пользователь:" FontSize="14" Margin="0,0,5,0"/>
                        <TextBlock x:Name="CurrentUserText" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                    </StackPanel>

                    <Button Content="🚪 Выйти из аккаунта" 
                            Click="LogoutButton_Click"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            Margin="0,10,0,0"/>
                </StackPanel>
            </materialDesign:Card>
        </StackPanel>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="💾 Сохранить" 
                    Click="SaveButton_Click"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Background="{DynamicResource PrimaryHueMidBrush}" 
                    Foreground="White"
                    Margin="0,0,10,0"/>
            <Button Content="❌ Закрыть" 
                    Click="CancelButton_Click"
                    Style="{StaticResource MaterialDesignOutlinedButton}"/>
        </StackPanel>
    </Grid>
</Window>